<div class="{{$classGrupoInput ? $classGrupoInput : ''}}">
    <label for="{{$for}}" class="{{$classLabel ? $classLabel : ''}}">{{$label}}</label>
    <input type="{{$type}}" id="{{$for}}" class="{{$classInput ? $classInput : ''}}">
</div>
